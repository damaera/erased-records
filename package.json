{
  "name": "erased-web",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "next dev",
    "build": "yarn re-build && next build",
    "start": "next start",
    "re-watch": "yarn re-clean && bsb -make-world -w",
    "re-build": "yarn re-clean && bsb -make-world",
    "re-clean": "bsb -clean-world"
  },
  "dependencies": {
    "@rescript/react": "^0.10.1",
    "bs-platform": "^9.0.2",
    "next": "10.1.3",
    "next-transpile-modules": "4",
    "react": "17.0.2",
    "react-dom": "17.0.2"
  }
}
